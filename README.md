# SYQEL Live — Releases

Installers and the updater feed (`latest.json`) for [SYQEL Live](https://syqel.com).
This repo holds only the build workflow and published artifacts — source lives in the
private repository. Releases are tag-triggered (`v*`); see the workflow for the build
matrix (macOS universal, Windows x64).
